一、编写入口页面，定义交易URI地址，定义好交易名称，由“交易码.后缀”构成，如：Query.act。

二、在交易配置文件“actionlist.xml”中，增加一个action节点，设置交易码、类全名、出口页面。

三、编写交易执行类，先做简单处理，并返回结果给输出页面。交易细节可以在1-5步跑通之后再回头写。

四、编写出口页面（有时与入口页面是同一个），完成结果展现。

五、以上步骤完成后，最后在首页菜单栏提供一个热链地址，链到入口页面。
